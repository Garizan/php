#  Лабораторная работа N2 
## Шаг 1. Загрузка PHP
Для установки PHP был использован официальный сайт:
https://www.php.net/downloads
Была загружена актуальная версия PHP для операционной системы Windows.

---
## Шаг 2. Распаковка архива
Скачанный архив был распакован в директорию:

В данной папке находится файл `php.exe`, который используется для запуска PHP из командной строки.

---

## Шаг 3. Добавление PHP в переменные среды

Для корректной работы PHP из любой директории был добавлен путь к PHP в системную переменную **Path**.

### Выполненные действия:

1. Открыто окно "Параметры системы"  
   (Win + R → `sysdm.cpl`)

2. Перейдено во вкладку **Дополнительно**

3. Открыт раздел **Переменные среды**

4. В разделе **Системные переменные** выбрана переменная `Path`

5. Добавлен путь:

6. Изменения сохранены

---

## Шаг 4. Проверка установки

Для проверки корректности установки выполнена команда в командной строке:

### Результат:

Отобразилась версия установленного PHP, например:

Это подтверждает, что PHP установлен и корректно добавлен в системные переменные.

---

## Шаг 3. Написание первой PHP-программы

Для выполнения задания была создана директория проекта:

```
D:\Универ\2курс\2семестр\php
```

В данной папке создан файл `index.php`, в который был добавлен следующий код:

```php
<?php

echo "Hello, World!";
```

Данный код выводит строку «Hello, World!» на экран.

---

### Запуск программы

Программа была запущена с помощью встроенного веб-сервера PHP командой:

```bash
php -S localhost:8000
```

После открытия в браузере адреса:

```
http://localhost:8000
```

на экране отобразился текст:

```
Hello, World!
```

Это подтверждает корректную работу PHP.

---

## Шаг 4. Вывод данных в PHP

В рамках задания была выполнена проверка вывода текста с использованием функций `echo` и `print`.

```php
<?php

echo "Hello, World with echo!<br />";
print "Hello, World with print!";
```

Функция `echo` выводит строку на экран.
Функция `print` также используется для вывода текста.

После запуска программы в браузере были успешно выведены строки:

```
Hello, World with echo!
Hello, World with print!
```

## Шаг 5. Работа с переменными и выводом

В ходе выполнения задания были созданы две переменные:

```php
<?php

$days = 288;
$message = "Все возвращаются на работу!";
```

Переменная `$days` является целочисленной и хранит значение 288.
Переменная `$message` является строковой и содержит текст: «Все возвращаются на работу!».

---

### Вывод с использованием конкатенации

Конкатенация — это объединение строк. В PHP для этого используется оператор `.`:

```php
echo $days . " " . $message;
```

В данном случае значения переменных объединяются в одну строку и выводятся на экран.

---

### Вывод с использованием двойных кавычек

При использовании двойных кавычек PHP автоматически подставляет значения переменных внутрь строки:

```php
echo "{$days} {$message}<br />";
```

Значения переменных выводятся без использования оператора конкатенации.

---

### Использование перехода на новую строку

Для переноса строки в браузере был использован HTML-тег:

```php
<br />
```

Это позволяет выводить данные с новой строки.

---

# Результат выполнения данной работы:

После запуска программы на экране отображается:

```
Hello world!
Hello, World with echo!
Hello, World with print
288 Все возвращаются на работу!
288 Все возвращаются на работу!
```

Вот краткие ответы для вставки в отчёт 👇

---

## Контрольные вопросы

### 1️. Какие способы установки PHP существуют?

Существует несколько способов установки PHP:

* Скачивание с официального сайта **php.net** и ручная установка.
* Установка через готовые сборки (XAMPP, OpenServer, WAMP).
* Установка через менеджеры пакетов (например, Chocolatey).
* Использование Docker-контейнера с PHP.

---

### 2️. Как проверить, что PHP установлен и работает?

Для проверки установки необходимо выполнить в командной строке команду:

```bash
php -v
```

Если отображается версия PHP, значит установка прошла успешно.

Также можно создать файл `index.php` с кодом:

```php
<?php echo "Test"; ?>
```

и запустить его через встроенный сервер PHP.

---

### 3️. Чем отличается оператор echo от print?

* `echo` используется для вывода текста и может выводить несколько значений.
* `print` выводит только одно значение.
* `echo` работает немного быстрее и чаще используется в практике.

---

##  Выводы

В ходе выполнения лабораторной работы была успешно установлена и настроена среда разработки PHP. Был изучен процесс добавления PHP в системные переменные и проверена корректность установки через командную строку. Создана и запущена первая PHP-программа, что позволило ознакомиться с базовым синтаксисом языка. Были рассмотрены способы вывода данных с помощью операторов `echo` и `print`, а также работа с переменными различных типов. Дополнительно изучены методы объединения строк и использование HTML-тега `<br />` для переноса строки в браузере. Полученные знания формируют основу для дальнейшего изучения языка программирования PHP и разработки веб-приложений.
